<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net11.0</TargetFramework>
  </PropertyGroup>

  <!-- The C# 15 and extension-member fixtures stay source-mode only: compiling them here
       would change this assembly's lowered member set and with it the method tables
       the tests expect. -->
  <ItemGroup>
    <Compile Remove="UnionTypes.cs" />
    <Compile Remove="CSharp15Features.cs" />
    <Compile Remove="ExtensionMembers.cs" />
  </ItemGroup>

</Project>
